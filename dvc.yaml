stages:
  preprocess:
    cmd: python scripts/preprocess.py
    deps:
    - data/raw
    outs:
    - data/processed

  evaluate:
    cmd: python scripts/evaluate.py
    deps:
    - models/model.pkl
    metrics:
    - metrics.json

  #deploy:
    #cmd: docker build -t maintenance-api . && docker run -p 5000:5000 maintenance-api
    #deps:
      #- scripts/api.py
      #- models/model.pkl
      #- Dockerfile
